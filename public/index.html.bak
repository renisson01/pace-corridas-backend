<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="PACE BRAZIL — O app de corrida de rua feito para o Brasil. IA Treinadora, GPS, ranking, comunidades e corridas.">
<meta property="og:title" content="PACE BRAZIL — A Casa do Corredor de Rua">
<meta property="og:description" content="Treine com IA. Acompanhe seu GPS. Dispute rankings. Encontre corridas. Tudo em um app.">
<meta name="theme-color" content="#00ffaa">
<title>PACE BRAZIL — A Casa do Corredor de Rua</title>
<link rel="manifest" href="/manifest.json">
<link rel="icon" href="/icons/icon-192.png">
<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700;800;900&family=Barlow:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #04060b;
  --bg2: #080c14;
  --bg3: #0c1220;
  --card: #0f1628;
  --card2: #141d32;
  --border: #1a2540;
  --neon: #00ffaa;
  --neon2: #00cc88;
  --neonDim: rgba(0,255,170,0.08);
  --orange: #ff6b00;
  --orange2: #ff8c33;
  --red: #ff2d55;
  --blue: #3b82f6;
  --yellow: #ffd60a;
  --text: #eeeef8;
  --muted: #6b7a99;
  --muted2: #8899bb;
  --display: 'Barlow Condensed', sans-serif;
  --body: 'Barlow', sans-serif;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{background:var(--bg);color:var(--text);font-family:var(--body);overflow-x:hidden;line-height:1.6}
a{color:inherit;text-decoration:none}

/* ═══ AMBIENT ═══ */
.ambient{position:fixed;inset:0;pointer-events:none;z-index:0;overflow:hidden}
.ambient-orb{position:absolute;border-radius:50%;filter:blur(100px);animation:orbFloat 20s ease-in-out infinite alternate}
.orb-1{width:600px;height:600px;top:-200px;left:-150px;background:rgba(0,255,170,0.06)}
.orb-2{width:500px;height:500px;bottom:-100px;right:-150px;background:rgba(255,107,0,0.05);animation-delay:-10s}
.orb-3{width:300px;height:300px;top:40%;left:40%;background:rgba(59,130,246,0.04);animation-delay:-5s}
@keyframes orbFloat{0%{transform:translate(0,0) scale(1)}100%{transform:translate(50px,30px) scale(1.15)}}

/* ═══ NAV ═══ */
nav{position:fixed;top:0;left:0;right:0;z-index:100;background:rgba(4,6,11,0.88);backdrop-filter:blur(20px);border-bottom:1px solid rgba(26,37,64,0.6);padding:0 24px;height:60px;display:flex;align-items:center;justify-content:space-between;transition:all 0.3s}
.nav-logo{font-family:var(--display);font-size:26px;font-weight:900;letter-spacing:4px;color:var(--neon);display:flex;align-items:center;gap:10px}
.nav-logo-icon{width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,var(--neon),var(--neon2));display:flex;align-items:center;justify-content:center;font-size:18px}
.nav-links{display:flex;gap:28px;align-items:center}
.nav-link{font-size:14px;font-weight:500;color:var(--muted2);transition:color 0.2s;cursor:pointer}
.nav-link:hover{color:var(--neon)}
.nav-cta{background:var(--neon);color:#000;padding:9px 20px;border-radius:10px;font-weight:700;font-size:13px;letter-spacing:0.5px;transition:all 0.2s;border:none;cursor:pointer;font-family:var(--body)}
.nav-cta:hover{background:var(--neon2);transform:translateY(-1px)}
.nav-mobile{display:none;background:none;border:none;color:var(--text);font-size:24px;cursor:pointer}

/* ═══ HERO ═══ */
.hero{position:relative;z-index:1;min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:100px 24px 60px;overflow:hidden}
.hero::before{content:'';position:absolute;bottom:0;left:0;right:0;height:200px;background:linear-gradient(to top,var(--bg),transparent);z-index:2}
.hero-badge{display:inline-flex;align-items:center;gap:8px;background:rgba(0,255,170,0.08);border:1px solid rgba(0,255,170,0.2);border-radius:30px;padding:6px 16px;font-size:12px;font-weight:700;color:var(--neon);letter-spacing:1.5px;text-transform:uppercase;margin-bottom:24px;animation:fadeUp 0.6s ease both}
.hero-badge .pulse{width:8px;height:8px;border-radius:50%;background:var(--neon);animation:pulse 2s infinite}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:0.5;transform:scale(0.8)}}
.hero-title{font-family:var(--display);font-size:clamp(48px,10vw,90px);font-weight:900;line-height:0.95;letter-spacing:-1px;margin-bottom:20px;animation:fadeUp 0.6s ease 0.1s both}
.hero-title .neon{color:var(--neon);display:block}
.hero-title .subtle{color:var(--muted2);font-weight:700;font-size:0.55em;letter-spacing:2px;display:block;margin-top:8px}
.hero-sub{font-size:18px;color:var(--muted2);max-width:520px;margin:0 auto 32px;line-height:1.7;animation:fadeUp 0.6s ease 0.2s both}
.hero-actions{display:flex;gap:14px;flex-wrap:wrap;justify-content:center;animation:fadeUp 0.6s ease 0.3s both;position:relative;z-index:3}
.btn-hero{padding:16px 36px;border-radius:14px;font-family:var(--display);font-size:18px;font-weight:800;letter-spacing:1px;cursor:pointer;transition:all 0.2s;border:none;display:flex;align-items:center;gap:10px}
.btn-primary{background:var(--neon);color:#000}
.btn-primary:hover{background:var(--neon2);transform:translateY(-2px);box-shadow:0 12px 40px rgba(0,255,170,0.25)}
.btn-secondary{background:transparent;color:var(--text);border:1.5px solid var(--border)}
.btn-secondary:hover{border-color:var(--neon);color:var(--neon)}
.hero-stats{display:flex;gap:40px;margin-top:48px;animation:fadeUp 0.6s ease 0.4s both;position:relative;z-index:3}
.hero-stat-num{font-family:var(--display);font-size:36px;font-weight:900;color:var(--neon)}
.hero-stat-label{font-size:12px;color:var(--muted);font-weight:600;letter-spacing:1px;text-transform:uppercase}
@keyframes fadeUp{from{opacity:0;transform:translateY(24px)}to{opacity:1;transform:translateY(0)}}

/* ═══ SECTION ═══ */
section{position:relative;z-index:1;padding:80px 24px}
.container{max-width:1100px;margin:0 auto}
.section-badge{display:inline-flex;align-items:center;gap:6px;background:var(--neonDim);border:1px solid rgba(0,255,170,0.15);border-radius:20px;padding:5px 14px;font-size:11px;font-weight:700;color:var(--neon);letter-spacing:2px;text-transform:uppercase;margin-bottom:16px}
.section-title{font-family:var(--display);font-size:clamp(32px,6vw,52px);font-weight:900;line-height:1.05;margin-bottom:16px}
.section-sub{font-size:16px;color:var(--muted2);max-width:600px;line-height:1.7}

/* ═══ FEATURES GRID ═══ */
.features-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:48px}
.feature-card{background:var(--card);border:1px solid var(--border);border-radius:20px;padding:28px 24px;position:relative;overflow:hidden;transition:all 0.3s;cursor:default}
.feature-card:hover{border-color:rgba(0,255,170,0.3);transform:translateY(-4px)}
.feature-card::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:var(--neon);opacity:0;transition:opacity 0.3s}
.feature-card:hover::before{opacity:1}
.feature-icon{width:52px;height:52px;border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:24px;margin-bottom:16px}
.feature-icon.green{background:rgba(0,255,170,0.1);border:1px solid rgba(0,255,170,0.2)}
.feature-icon.orange{background:rgba(255,107,0,0.1);border:1px solid rgba(255,107,0,0.2)}
.feature-icon.blue{background:rgba(59,130,246,0.1);border:1px solid rgba(59,130,246,0.2)}
.feature-icon.yellow{background:rgba(255,214,10,0.1);border:1px solid rgba(255,214,10,0.2)}
.feature-icon.red{background:rgba(255,45,85,0.1);border:1px solid rgba(255,45,85,0.2)}
.feature-icon.purple{background:rgba(167,139,250,0.1);border:1px solid rgba(167,139,250,0.2)}
.feature-name{font-family:var(--display);font-size:20px;font-weight:800;margin-bottom:6px;letter-spacing:0.5px}
.feature-desc{font-size:13px;color:var(--muted2);line-height:1.6}

/* ═══ PRICING ═══ */
.pricing-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px;margin-top:48px;align-items:start}
.price-card{background:var(--card);border:1px solid var(--border);border-radius:24px;padding:32px 28px;position:relative;overflow:hidden;transition:all 0.3s}
.price-card.featured{border-color:var(--neon);background:linear-gradient(180deg,rgba(0,255,170,0.04),var(--card))}
.price-card.featured::before{content:'';position:absolute;top:0;left:0;right:0;height:4px;background:linear-gradient(90deg,var(--neon),var(--blue))}
.price-card.coach{border-color:var(--orange)}
.price-card.coach::before{content:'';position:absolute;top:0;left:0;right:0;height:4px;background:linear-gradient(90deg,var(--orange),var(--yellow))}
.price-tag{display:inline-block;padding:4px 12px;border-radius:10px;font-size:11px;font-weight:800;letter-spacing:1px;text-transform:uppercase;margin-bottom:16px}
.tag-free{background:rgba(107,122,153,0.2);color:var(--muted2)}
.tag-premium{background:rgba(0,255,170,0.12);color:var(--neon)}
.tag-coach{background:rgba(255,107,0,0.12);color:var(--orange)}
.price-name{font-family:var(--display);font-size:26px;font-weight:900;margin-bottom:6px}
.price-amount{font-family:var(--display);font-size:42px;font-weight:900;margin-bottom:4px}
.price-amount .currency{font-size:20px;color:var(--muted2);font-weight:700}
.price-amount .period{font-size:16px;color:var(--muted);font-weight:600}
.price-amount.green{color:var(--neon)}
.price-amount.orange{color:var(--orange)}
.price-detail{font-size:13px;color:var(--muted2);margin-bottom:20px}
.price-features{list-style:none;margin-bottom:24px}
.price-features li{font-size:13px;color:var(--muted2);padding:6px 0;display:flex;align-items:center;gap:8px;border-bottom:1px solid rgba(26,37,64,0.5)}
.price-features li:last-child{border-bottom:none}
.price-features .check{color:var(--neon);font-weight:700;flex-shrink:0}
.price-features .check.orange{color:var(--orange)}
.price-features .lock{color:var(--muted);font-size:11px}
.btn-price{width:100%;padding:14px;border-radius:14px;font-family:var(--display);font-size:17px;font-weight:800;letter-spacing:0.5px;cursor:pointer;transition:all 0.2s;border:none}
.btn-price.green{background:var(--neon);color:#000}
.btn-price.green:hover{background:var(--neon2);transform:translateY(-2px);box-shadow:0 8px 30px rgba(0,255,170,0.2)}
.btn-price.orange{background:var(--orange);color:#000}
.btn-price.orange:hover{background:var(--orange2);transform:translateY(-2px);box-shadow:0 8px 30px rgba(255,107,0,0.2)}
.btn-price.ghost{background:transparent;color:var(--muted2);border:1px solid var(--border)}
.btn-price.ghost:hover{border-color:var(--neon);color:var(--neon)}
.guarantee{display:flex;align-items:center;gap:8px;margin-top:14px;font-size:12px;color:var(--neon);font-weight:600}
.guarantee-icon{font-size:16px}

/* ═══ SOCIAL PROOF ═══ */
.proof-strip{background:var(--bg2);border-top:1px solid var(--border);border-bottom:1px solid var(--border);padding:40px 24px;text-align:center}
.proof-logos{display:flex;align-items:center;justify-content:center;gap:40px;flex-wrap:wrap;margin-top:20px}
.proof-item{display:flex;flex-direction:column;align-items:center;gap:4px}
.proof-num{font-family:var(--display);font-size:32px;font-weight:900;color:var(--neon)}
.proof-label{font-size:11px;color:var(--muted);font-weight:600;letter-spacing:1px;text-transform:uppercase}

/* ═══ CTA FINAL ═══ */
.cta-section{text-align:center;padding:100px 24px}
.cta-card{background:linear-gradient(135deg,rgba(0,255,170,0.05),rgba(59,130,246,0.03));border:1px solid rgba(0,255,170,0.15);border-radius:28px;padding:60px 40px;max-width:700px;margin:0 auto;position:relative;overflow:hidden}
.cta-card::before{content:'';position:absolute;top:0;left:0;right:0;height:4px;background:linear-gradient(90deg,var(--neon),var(--blue),var(--orange))}
.cta-title{font-family:var(--display);font-size:clamp(28px,5vw,44px);font-weight:900;margin-bottom:12px}
.cta-sub{font-size:16px;color:var(--muted2);margin-bottom:32px;max-width:480px;margin-left:auto;margin-right:auto}
.cta-buttons{display:flex;gap:14px;justify-content:center;flex-wrap:wrap}

/* ═══ FOOTER ═══ */
footer{background:var(--bg2);border-top:1px solid var(--border);padding:40px 24px;text-align:center}
.footer-logo{font-family:var(--display);font-size:24px;font-weight:900;letter-spacing:4px;color:var(--neon);margin-bottom:8px}
.footer-sub{font-size:13px;color:var(--muted);margin-bottom:20px}
.footer-links{display:flex;gap:20px;justify-content:center;margin-bottom:20px}
.footer-links a{font-size:13px;color:var(--muted2);transition:color 0.2s}
.footer-links a:hover{color:var(--neon)}
.footer-copy{font-size:11px;color:var(--muted)}

/* ═══ MOBILE ═══ */
@media(max-width:900px){
  .nav-links{display:none}
  .nav-mobile{display:block}
  .features-grid{grid-template-columns:1fr}
  .pricing-grid{grid-template-columns:1fr}
  .hero-stats{gap:24px}
  .hero-stat-num{font-size:28px}
  .hero-sub{font-size:16px}
  .btn-hero{padding:14px 28px;font-size:16px}
}
@media(max-width:480px){
  .hero-stats{flex-direction:row;flex-wrap:wrap;gap:20px;justify-content:center}
  .hero-actions{flex-direction:column;align-items:stretch;padding:0 20px}
  .cta-card{padding:40px 24px}
  .proof-logos{gap:24px}
}
</style>
</head>
<body>

<!-- AMBIENT -->
<div class="ambient">
  <div class="ambient-orb orb-1"></div>
  <div class="ambient-orb orb-2"></div>
  <div class="ambient-orb orb-3"></div>
</div>

<!-- NAV -->
<nav>
  <a href="/" class="nav-logo">
    <div class="nav-logo-icon">🏃</div>
    PACE
  </a>
  <div class="nav-links">
    <a href="#funcionalidades" class="nav-link">Funcionalidades</a>
    <a href="#planos" class="nav-link">Planos</a>
    <a href="#treinadores" class="nav-link">Treinadores</a>
    <a href="/entrar.html" class="nav-cta">Criar Conta Grátis</a>
  </div>
  <button class="nav-mobile" onclick="window.location.href='/entrar.html'">Entrar</button>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-badge"><span class="pulse"></span> A plataforma #1 do corredor brasileiro</div>
  <h1 class="hero-title">
    A CASA DO<br>
    <span class="neon">CORREDOR DE RUA</span>
    <span class="subtle">Treine. Evolua. Conquiste.</span>
  </h1>
  <p class="hero-sub">Sua treinadora IA pessoal, GPS integrado, ranking nacional, corridas abertas e comunidade. Tudo que o corredor brasileiro precisa em um app.</p>
  <div class="hero-actions">
    <a href="/entrar.html" class="btn-hero btn-primary">🚀 Comece Grátis</a>
    <a href="#planos" class="btn-hero btn-secondary">Ver Planos</a>
  </div>
  <div class="hero-stats">
    <div><div class="hero-stat-num">500+</div><div class="hero-stat-label">Corridas</div></div>
    <div><div class="hero-stat-num">1.2K+</div><div class="hero-stat-label">Atletas</div></div>
    <div><div class="hero-stat-num">26</div><div class="hero-stat-label">Estados</div></div>
  </div>
</section>

<!-- FUNCIONALIDADES -->
<section id="funcionalidades">
  <div class="container">
    <div class="section-badge">✨ Tudo em um app</div>
    <div class="section-title">Feito para quem<br>corre de verdade</div>
    <div class="section-sub">Cada funcionalidade foi pensada por corredores, para corredores. Sem enrolação.</div>

    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon green">🤖</div>
        <div class="feature-name">IA Treinadora</div>
        <div class="feature-desc">Sua treinadora pessoal 24h. Planilhas semanais, zonas de FC, análise de evolução e orientação nutricional. Tudo personalizado pro seu nível.</div>
      </div>
      <div class="feature-card">
        <div class="feature-icon orange">📍</div>
        <div class="feature-name">GPS de Corrida</div>
        <div class="feature-desc">Registre seus treinos com pace em tempo real, rota, elevação e calorias. Sincronize com Strava, Garmin e Polar.</div>
      </div>
      <div class="feature-card">
        <div class="feature-icon yellow">🏆</div>
        <div class="feature-name">Ranking Nacional</div>
        <div class="feature-desc">Veja sua posição entre os corredores do Brasil. Rankings por distância (3km, 5km, 10km, 21km, 42km) e faixa etária.</div>
      </div>
      <div class="feature-card">
        <div class="feature-icon blue">🏁</div>
        <div class="feature-name">Corridas Abertas</div>
        <div class="feature-desc">Calendário completo de corridas de rua em todos os estados. Inscrições, kits, regulamentos e distâncias.</div>
      </div>
      <div class="feature-card">
        <div class="feature-icon red">👥</div>
        <div class="feature-name">Comunidade</div>
        <div class="feature-desc">Assessorias, grupos de treino, feed social e amigos. Encontre quem corre perto de você e treinem juntos.</div>
      </div>
      <div class="feature-card">
        <div class="feature-icon purple">🛒</div>
        <div class="feature-name">Loja do Corredor</div>
        <div class="feature-desc">Camisetas técnicas, acessórios e kits. Produtos exclusivos para assessorias e equipes personalizados.</div>
      </div>
    </div>
  </div>
</section>

<!-- SOCIAL PROOF -->
<div class="proof-strip">
  <div style="font-family:var(--display);font-size:18px;font-weight:800;letter-spacing:2px;color:var(--muted2)">NÚMEROS QUE FALAM</div>
  <div class="proof-logos">
    <div class="proof-item"><div class="proof-num">500+</div><div class="proof-label">Corridas cadastradas</div></div>
    <div class="proof-item"><div class="proof-num">26</div><div class="proof-label">Estados cobertos</div></div>
    <div class="proof-item"><div class="proof-num">24h</div><div class="proof-label">IA disponível</div></div>
    <div class="proof-item"><div class="proof-num">100%</div><div class="proof-label">Brasileiro</div></div>
  </div>
</div>

<!-- PLANOS -->
<section id="planos">
  <div class="container" style="text-align:center">
    <div class="section-badge">💰 Planos</div>
    <div class="section-title">Escolha seu ritmo</div>
    <div class="section-sub" style="margin:0 auto 0">Comece grátis e evolua quando quiser. Garantia de 7 dias em todos os planos pagos.</div>

    <div class="pricing-grid">
      <!-- FREE -->
      <div class="price-card">
        <div class="price-tag tag-free">Gratuito</div>
        <div class="price-name">Atleta</div>
        <div class="price-amount"><span class="currency">R$</span> 0</div>
        <div class="price-detail">Para sempre. Sem cartão.</div>
        <ul class="price-features">
          <li><span class="check">✓</span> Ranking nacional</li>
          <li><span class="check">✓</span> Corridas abertas</li>
          <li><span class="check">✓</span> GPS de corrida</li>
          <li><span class="check">✓</span> Comunidade e grupos</li>
          <li><span class="check">✓</span> IA Treinadora (5 msgs/hora)</li>
          <li><span class="lock">🔒</span> <span style="color:var(--muted)">Planilha personalizada</span></li>
          <li><span class="lock">🔒</span> <span style="color:var(--muted)">Análise completa de evolução</span></li>
        </ul>
        <a href="/entrar.html" class="btn-price ghost">Criar Conta Grátis</a>
      </div>

      <!-- PREMIUM -->
      <div class="price-card featured">
        <div class="price-tag tag-premium">⭐ Mais popular</div>
        <div class="price-name">Premium</div>
        <div class="price-amount green"><span class="currency">R$</span> 29<span class="period">,90/mês</span></div>
        <div class="price-detail">IA Treinadora completa + recursos avançados</div>
        <ul class="price-features">
          <li><span class="check">✓</span> <strong>Tudo do plano Gratuito</strong></li>
          <li><span class="check">✓</span> 60 mensagens/hora com IA</li>
          <li><span class="check">✓</span> Planilha semanal personalizada</li>
          <li><span class="check">✓</span> Análise completa de evolução</li>
          <li><span class="check">✓</span> Chance de pódio por faixa etária</li>
          <li><span class="check">✓</span> Nutrição e prevenção de lesões</li>
          <li><span class="check">✓</span> Suporte prioritário</li>
        </ul>
        <a href="/entrar.html?plan=premium" class="btn-price green">⭐ Assinar Premium</a>
        <div class="guarantee"><span class="guarantee-icon">🛡️</span> 7 dias de garantia. Não gostou? Devolvemos.</div>
      </div>

      <!-- COACH -->
      <div class="price-card coach" id="treinadores">
        <div class="price-tag tag-coach">🏋️ Para profissionais</div>
        <div class="price-name">Treinador</div>
        <div class="price-amount orange"><span class="currency">R$</span> 99<span class="period">,90 adesão</span></div>
        <div class="price-detail">+ R$ 3,99/atleta por mês</div>
        <ul class="price-features">
          <li><span class="check orange">✓</span> Painel profissional completo</li>
          <li><span class="check orange">✓</span> Criar planilhas por atleta</li>
          <li><span class="check orange">✓</span> Ver GPS e atividades dos atletas</li>
          <li><span class="check orange">✓</span> Calculadora de zonas FC e paces</li>
          <li><span class="check orange">✓</span> IA Coach assistente</li>
          <li><span class="check orange">✓</span> Link de cadastro exclusivo</li>
          <li><span class="check orange">✓</span> Biblioteca de treinos</li>
        </ul>
        <a href="/entrar.html?role=coach" class="btn-price orange">🏋️ Quero ser Treinador PACE</a>
        <div class="guarantee" style="color:var(--orange)"><span class="guarantee-icon">🛡️</span> 7 dias de garantia na adesão</div>
      </div>
    </div>

    <div style="margin-top:32px;font-size:13px;color:var(--muted)">
      10 atletas = R$ 39,90/mês · 20 atletas = R$ 79,80/mês · 50 atletas = R$ 199,50/mês
    </div>
  </div>
</section>

<!-- CTA FINAL -->
<section class="cta-section">
  <div class="cta-card">
    <div style="font-size:48px;margin-bottom:16px">🏃</div>
    <div class="cta-title">Pronto pra correr<br>no próximo nível?</div>
    <div class="cta-sub">Mais de 1.000 corredores já estão evoluindo com o PACE. Sua vez de cruzar a linha de chegada.</div>
    <div class="cta-buttons">
      <a href="/entrar.html" class="btn-hero btn-primary">🚀 Comece Agora — É Grátis</a>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">PACE BRAZIL</div>
  <div class="footer-sub">A Casa do Corredor de Rua</div>
  <div class="footer-links">
    <a href="/entrar.html">Entrar</a>
    <a href="#funcionalidades">Funcionalidades</a>
    <a href="#planos">Planos</a>
    <a href="#treinadores">Treinadores</a>
  </div>
  <div class="footer-copy">© 2026 PACE BRAZIL. Todos os direitos reservados. Feito com 💚 para corredores.</div>
</footer>

<script>
// Smooth scroll
document.querySelectorAll('a[href^="#"]').forEach(a => {
  a.addEventListener('click', e => {
    e.preventDefault();
    document.querySelector(a.getAttribute('href'))?.scrollIntoView({behavior:'smooth'});
  });
});

// Nav background on scroll
window.addEventListener('scroll', () => {
  const nav = document.querySelector('nav');
  if (window.scrollY > 50) nav.style.background = 'rgba(4,6,11,0.96)';
  else nav.style.background = 'rgba(4,6,11,0.88)';
});

// Animate on scroll
const obs = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) { e.target.style.opacity = '1'; e.target.style.transform = 'translateY(0)'; }
  });
}, {threshold:0.1});
document.querySelectorAll('.feature-card, .price-card').forEach(el => {
  el.style.opacity = '0'; el.style.transform = 'translateY(30px)'; el.style.transition = 'all 0.5s ease';
  obs.observe(el);
});
</script>
</body>
</html>
